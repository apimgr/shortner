Subject: Update Available - {app_name}
---
UPDATE AVAILABLE

From: {app_name} ({fqdn})
Time: {timestamp}

A newer release is available on the {channel} channel.

Current version: {current_version}
New version: {new_version}

Install it by running the server binary with --update yes on this host.

You are receiving this because update_available email notifications are
enabled in server.notifications.email.events.

--
{app_name}
{app_url}
